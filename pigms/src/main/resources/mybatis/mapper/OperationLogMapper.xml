<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhu.mapper.OperationLogMapper">
    
    <resultMap id="BaseResultMap" type="com.zhu.pojo.OperationLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="ip" property="ip"/>
        <result column="execute_time" property="executeTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.zhu.pojo.OperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_operation_log 
        (user_id, username, operation, method, params, ip, execute_time, create_time)
        VALUES 
        (#{userId}, #{username}, #{operation}, #{method}, #{params}, #{ip}, #{executeTime}, #{createTime})
    </insert>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM tb_operation_log WHERE id = #{id}
    </delete>

    <delete id="deleteByCreateTime" parameterType="java.util.Date">
        DELETE FROM tb_operation_log WHERE create_time &lt; #{beforeDate}
    </delete>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM tb_operation_log ORDER BY create_time DESC
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM tb_operation_log
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="operation != null and operation != ''">
                AND operation LIKE CONCAT('%', #{operation}, '%')
            </if>
            <if test="startTime != null">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>



